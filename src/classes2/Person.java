package classes2;

//An interface is a completely "abstract class" that is used to group related methods with empty bodies

interface Person {

    String GetFullName(); // interface method (does not have a body)

    int GetAge(); // interface method (does not have a body)
}
